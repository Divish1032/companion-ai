# Companion AI

Low-latency Hindi/Hinglish voice-only AI companion MVP for Android and iOS.

## Start Here

- `AGENTS.md`: repository-wide rules for coding agents and humans.
- `docs/README.md`: documentation map.
- `docs/AGENT_CONTEXT.md`: active sprint rules and reading guide.
- `docs/SPRINTS.md`: execution plan.

## Current Status

Active phase: Sprint -1 validation gates.

Do not start app/backend scaffolding until Sprint -1 outputs are created or the project owner explicitly moves the project forward.

## Useful Commands

```bash
make check
```

## Git Hooks

This repo uses local hooks from `.githooks/`.

If hooks are not active, run:

```bash
git config core.hooksPath .githooks
git config commit.template .gitmessage
```

